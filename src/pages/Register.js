const register = ()=>{
    return
        <main>
            <form id ="formulario">
                <label for= "nomeCompleto">Nome completo</label>
                <input name="nomeCompleto"type="text" required />
            </form>
        </main>
    
}